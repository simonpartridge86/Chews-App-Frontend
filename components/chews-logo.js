export default function ChewsLogo({ fontSize }) {
  return (
    <h1
      className={
        "font-permanent-marker " + "text-primary-color " + "text-" + fontSize
      }
    >
      Chews
    </h1>
  );
}
